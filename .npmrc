# Vue 2 与 Vue 3 在同一仓库共存:@vitejs/plugin-vue2 要求 peer vue@^2.7,
# 而本仓库 dev 依赖 vue@3 用于 Vue 3 壳/demo。npm 严格 peer 解析会判冲突,
# 故 dev/CI 安装统一走 legacy-peer-deps。
# 注意:此文件不在 package.json 的 files 白名单中,不会随 npm publish 发给使用者,
# 也不改变发布包的运行时依赖,仅影响本仓库的 install 行为。
legacy-peer-deps=true
